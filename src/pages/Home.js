import WelcomePage from './../components/WelcomePage/WelcomePage';

const Home = () => {
  // if (auth.user) {
  //   history.push('/room/new');
  // }

  return (
    <div>
      <WelcomePage />
    </div>
  );
};

export default Home;
